#Task runner with autoupdate last version of repo and restart cmd. Repo must be clonned and cmd must be run in repo as working dir

### example
gitwatcher -token 'github token with read rights' -deploy (deploy command) 'go run ./cmd/teletalkie'
